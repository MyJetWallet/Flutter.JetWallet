import 'package:flutter/material.dart';

class SkeletonSecondSide extends StatelessWidget {
  const SkeletonSecondSide({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

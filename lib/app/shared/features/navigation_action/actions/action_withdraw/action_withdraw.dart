import 'package:flutter/material.dart';

class ActionWithdraw extends StatelessWidget {
  const ActionWithdraw({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text(
          'Action Withdraw flow',
        ),
      ),
    );
  }
}

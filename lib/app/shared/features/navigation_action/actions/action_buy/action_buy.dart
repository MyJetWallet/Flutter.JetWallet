import 'package:flutter/material.dart';

class ActionBuy extends StatelessWidget {
  const ActionBuy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text(
          'Action Buy flow',
        ),
      ),
    );
  }
}

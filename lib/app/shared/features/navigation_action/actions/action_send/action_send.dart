import 'package:flutter/material.dart';

class ActionSend extends StatelessWidget {
  const ActionSend({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text(
          'Action Send flow',
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class ActionDeposit extends StatelessWidget {
  const ActionDeposit({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text(
          'Action Depoist flow',
        ),
      ),
    );
  }
}

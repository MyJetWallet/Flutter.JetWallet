import 'package:flutter/material.dart';

class CurrencySell extends StatelessWidget {
  const CurrencySell({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text(
          'Currency Sell flow',
        ),
      ),
    );
  }
}

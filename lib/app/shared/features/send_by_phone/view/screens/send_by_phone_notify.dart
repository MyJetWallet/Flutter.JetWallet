import 'package:flutter/material.dart';

class SendByPhoneNotify extends StatelessWidget {
  const SendByPhoneNotify({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

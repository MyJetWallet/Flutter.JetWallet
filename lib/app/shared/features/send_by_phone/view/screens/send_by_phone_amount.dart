import 'package:flutter/material.dart';

class SendByPhoneAmount extends StatelessWidget {
  const SendByPhoneAmount({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

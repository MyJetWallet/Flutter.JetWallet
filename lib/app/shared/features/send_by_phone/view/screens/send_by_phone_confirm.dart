import 'package:flutter/material.dart';

class SendByPhoneConfirm extends StatelessWidget {
  const SendByPhoneConfirm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

import 'package:flutter/material.dart';

class SendByPhonePreview extends StatelessWidget {
  const SendByPhonePreview({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

import 'package:flutter/material.dart';

class SimpleConfirmationCodeFieldExample extends StatelessWidget {
  const SimpleConfirmationCodeFieldExample({Key? key}) : super(key: key);

  static const routeName = '/simple_confirmation_code_field_example';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}

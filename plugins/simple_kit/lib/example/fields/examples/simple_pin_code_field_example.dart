import 'package:flutter/material.dart';

class SimplePinCodeFieldExample extends StatelessWidget {
  const SimplePinCodeFieldExample({Key? key}) : super(key: key);

  static const routeName = '/simple_pin_code_field_example';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}

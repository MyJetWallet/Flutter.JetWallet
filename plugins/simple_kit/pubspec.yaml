name: simple_kit
description: UI Kit for Simple design

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
    
  flutter_screenutil: ^5.0.0+2 # responsive support
  flutter_riverpod: ^0.14.0+3 # state management
  flutter_hooks: ^0.18.0 # manages a Widget life-cycles
  flutter_svg: ^0.22.0 # SVG rendering and widget library

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.7.2

flutter:
  assets:
    - assets/icons/light/mail/
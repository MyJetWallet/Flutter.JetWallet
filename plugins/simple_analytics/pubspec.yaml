name: simple_analytics
description: Library for Analytics.
version: 0.0.1

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  crypto: ^3.0.6
  amplitude_flutter: ^3.16.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^2.3.0

flutter:

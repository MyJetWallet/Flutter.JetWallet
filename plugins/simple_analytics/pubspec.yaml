name: simple_analytics
description: Library for Analytics.
version: 0.0.1

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  crypto: ^3.0.1
  amplitude_flutter: ^3.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.8.1

flutter:
